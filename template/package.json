{
  "name": "<%= name %>",
  "version": "0.0.1",
  "description": "<%= description %>",
  "repository": {
    "url": "<%= username %>/<%= name %>",
    "type": "git"
  },
  "scripts": {
    "dev": "vbuild -dc <% if (eslint) { %>--eslint<% } %>",
    "build": "vbuild -c"
  },
  "author": "<%= username %> <<%= email %>>",
  "license": "MIT",
  "devDependencies": {
    "babel-plugin-react-require": "^3.0.0",
    "babel-preset-react-app": "^2.1.1",
    "react-hot-loader": "next"<% if (vbuild) { %>,
    "vbuild": "^6.24.0"<% } %>
  },
  "dependencies": {
    "react": "^15.4.2",
    "react-dom": "^15.4.2"
  }
}

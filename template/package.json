{
  "name": "<%= name %>",
  "version": "0.0.1",
  "description": "<%= description %>",
  "repository": {
    "url": "<%= username %>/<%= name %>",
    "type": "git"
  },
  "scripts": {
    "dev": "poi",
    "test": "<%= test ? 'react-jest --env=jsdom':'echo \'no test\'' %>",
    "build": "poi build"
  },
  "author": "<%= username %> <<%= email %>>",
  "license": "MIT",
  "devDependencies": {
    "babel-plugin-react-require": "^3.0.0",
    "babel-preset-react-app": "^2.1.1",
    "babel-plugin-transform-decorators-legacy": "^1.3.4",<% if (test) { %>
    "react-jest": "^0.2.0",<% } %><% if (enzyme) { %>
    "react-test-renderer": "^15.5.4",
    "enzyme": "^2.8.2",<% } %>
    "react-hot-loader": "next",
    "poi": "^8.0.5"<% if (lint !== 'never') { %>,
    "poi-preset-eslint-react": "^0.0.2"<% } %>
  },
  "dependencies": {
    "react": "^15.4.2",
    "react-dom": "^15.4.2"
  }
}

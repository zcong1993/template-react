{
  "name": "<%= name %>",
  "productName": "<%= name %>",
  "version": "0.0.1",
  "description": "<%= description %>",
  "repository": {
    "url": "<%= username %>/<%= name %>",
    "type": "git"
  },
  "scripts": {
    "dev": "poi",
    "test": "<% if (test) { %>react-jest --env=jsdom<% }else{ %>echo 'no test'<%}%>",
    "build": "poi build"
  },
  "author": "<%= username %> <<%= email %>>",
  "license": "MIT",
  "devDependencies": {
    "babel-plugin-react-require": "^3.0.0",
    "babel-preset-react-app": "^3.0.0",
    "babel-plugin-transform-decorators-legacy": "^1.3.4",<% if (test) { %>
    "react-jest": "^0.2.1",<% } %><% if (enzyme) { %>
    "react-test-renderer": "^15.5.4",
    "enzyme": "^2.8.2",<% } %>
    "react-hot-loader": "next",
    "poi": "^8.1.2"<% if (lint !== 'never') { %>,
    "poi-preset-eslint-react": "^0.0.3"<% } %><% if(pwa) { %>,
    "poi-preset-offline": "^8.0.3",
    "offline-plugin": "^4.8.1"<% } %>
  },
  "dependencies": {
    "react": "^15.5.4",
    "react-dom": "^15.5.4"
  }
}

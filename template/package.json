{
  "name": "<%= name %>",
  "version": "0.0.1",
  "description": "<%= description %>",
  "repository": {
    "url": "<%= username %>/<%= name %>",
    "type": "git"
  },
  "scripts": {
    "dev": "vbuild dev",
    "build": "vbuild"
  },
  "author": "<%= username %> <<%= email %>>",
  "license": "MIT",
  "devDependencies": {
    "babel-plugin-react-require": "^3.0.0",
    "babel-preset-react-app": "^2.1.1",
    "babel-plugin-transform-decorators-legacy": "^1.3.4",
    "react-hot-loader": "next"<% if (vbuild || offline) { %>,
    "vbuild": "^7"<% } %><% if (offline) { %>,
    "offline-plugin": "^4.6.1"<% } %>
  },
  "dependencies": {
    "react": "^15.4.2",
    "react-dom": "^15.4.2"
  },
  "xo": {
    "parser": "babel-eslint"
  }
}
